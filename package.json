{
  "private": true,
  "config": {
    "commitizen": {
      "path": "cz-conventional-changelog"
    }
  },
  "devDependencies": {
    "husky": "^0.14.3",
    "lerna": "^3.10.2"
  },
  "scripts": {
    "precommit": "lerna run --concurrency 1 --stream precommit"
  },
  "dependencies": {
    "debug": "^3.1.0"
  }
}
